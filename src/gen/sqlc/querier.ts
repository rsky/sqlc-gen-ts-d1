// Code generated by sqlc-gen-ts-do-sqlstorage. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
//   sqlc-gen-ts-do-sqlstorage v0.0.0-a@604babe3ba8502260e741f0a05b811b875f67db9354b5c2907468464eb8cbab2

import { Account } from "./models"

type Query<T> = {
  then(onFulfilled?: (value: T) => void, onRejected?: (reason?: any) => void): void;
  batch(): SqlStorageStatement;
}
const getAccountQuery = `-- name: GetAccount :one
SELECT pk, id, display_name, email FROM account WHERE id = ?1`;

export type GetAccountParams = {
  accountId: string;
};

export type GetAccountRow = {
  pk: number;
  id: string;
  displayName: string;
  email: string | null;
};

type RawGetAccountRow = {
  pk: number;
  id: string;
  display_name: string;
  email: string | null;
};

export function getAccount(
  sql: SqlStorage,
  args: GetAccountParams
): Query<GetAccountRow | null> {
  const ps = sql
    .prepare(getAccountQuery)
    .bind(args.accountId);
  return {
    then(onFulfilled?: (value: GetAccountRow | null) => void, onRejected?: (reason?: any) => void) {
      ps.first<RawGetAccountRow | null>()
        .then((raw: RawGetAccountRow | null) => raw ? {
          pk: raw.pk,
          id: raw.id,
          displayName: raw.display_name,
          email: raw.email,
        } : null)
        .then(onFulfilled).catch(onRejected);
    },
    batch() { return ps; },
  }
}

const listAccountsQuery = `-- name: ListAccounts :many
SELECT account.pk AS account_pk, account.id AS account_id, account.display_name AS account_display_name, account.email AS account_email FROM account`;

export type ListAccountsRow = {
  account: Account;
};

type RawListAccountsRow = {
  account_pk: number;
  account_id: string;
  account_display_name: string;
  account_email: string | null;
};

export function listAccounts(
  sql: SqlStorage
): Query<SqlStorageCursor<ListAccountsRow>> {
  const ps = sql
    .prepare(listAccountsQuery);
  return {
    then(onFulfilled?: (value: SqlStorageCursor<ListAccountsRow>) => void, onRejected?: (reason?: any) => void) {
      ps.all<RawListAccountsRow>()
        .then((r: SqlStorageCursor<RawListAccountsRow>) => { return {
          ...r,
          results: r.results.map((raw: RawListAccountsRow) => { return {
            // sqlc.embed(account)
            account: {
              pk: raw.account_pk,
              id: raw.account_id,
              displayName: raw.account_display_name,
              email: raw.account_email,
            },
          }}),
        }})
        .then(onFulfilled).catch(onRejected);
    },
    batch() { return ps; },
  }
}

const createAccountQuery = `-- name: CreateAccount :exec
INSERT INTO account (id, display_name, email)
VALUES (?1, ?2, ?3)`;

export type CreateAccountParams = {
  id: string;
  displayName: string;
  email: string | null;
};

export function createAccount(
  sql: SqlStorage,
  args: CreateAccountParams
): Query<SqlStorageCursor> {
  const ps = sql
    .prepare(createAccountQuery)
    .bind(args.id, args.displayName, args.email);
  return {
    then(onFulfilled?: (value: SqlStorageCursor) => void, onRejected?: (reason?: any) => void) {
      ps.run()
        .then(onFulfilled).catch(onRejected);
    },
    batch() { return ps; },
  }
}

const updateAccountDisplayNameQuery = `-- name: UpdateAccountDisplayName :one
UPDATE account
SET display_name = ?1
WHERE id = ?2
RETURNING pk, id, display_name, email`;

export type UpdateAccountDisplayNameParams = {
  displayName: string;
  id: string;
};

export type UpdateAccountDisplayNameRow = {
  pk: number;
  id: string;
  displayName: string;
  email: string | null;
};

type RawUpdateAccountDisplayNameRow = {
  pk: number;
  id: string;
  display_name: string;
  email: string | null;
};

export function updateAccountDisplayName(
  sql: SqlStorage,
  args: UpdateAccountDisplayNameParams
): Query<UpdateAccountDisplayNameRow | null> {
  const ps = sql
    .prepare(updateAccountDisplayNameQuery)
    .bind(args.displayName, args.id);
  return {
    then(onFulfilled?: (value: UpdateAccountDisplayNameRow | null) => void, onRejected?: (reason?: any) => void) {
      ps.first<RawUpdateAccountDisplayNameRow | null>()
        .then((raw: RawUpdateAccountDisplayNameRow | null) => raw ? {
          pk: raw.pk,
          id: raw.id,
          displayName: raw.display_name,
          email: raw.email,
        } : null)
        .then(onFulfilled).catch(onRejected);
    },
    batch() { return ps; },
  }
}

const getAccountsQuery = `-- name: GetAccounts :many
SELECT pk, id, display_name, email FROM account WHERE id IN (/*SLICE:ids*/?)`;

export type GetAccountsParams = {
  ids: string[];
};

export type GetAccountsRow = {
  pk: number;
  id: string;
  displayName: string;
  email: string | null;
};

type RawGetAccountsRow = {
  pk: number;
  id: string;
  display_name: string;
  email: string | null;
};

export function getAccounts(
  sql: SqlStorage,
  args: GetAccountsParams
): Query<SqlStorageCursor<GetAccountsRow>> {
  let query = getAccountsQuery;
  const params: any[] = [args.ids[0]];
  query = query.replace("(/*SLICE:ids*/?)", expandedParam(1, args.ids.length, params.length));
  params.push(...args.ids.slice(1));
  const ps = sql
    .prepare(query)
    .bind(...params);
  return {
    then(onFulfilled?: (value: SqlStorageCursor<GetAccountsRow>) => void, onRejected?: (reason?: any) => void) {
      ps.all<RawGetAccountsRow>()
        .then((r: SqlStorageCursor<RawGetAccountsRow>) => { return {
          ...r,
          results: r.results.map((raw: RawGetAccountsRow) => { return {
            pk: raw.pk,
            id: raw.id,
            displayName: raw.display_name,
            email: raw.email,
          }}),
        }})
        .then(onFulfilled).catch(onRejected);
    },
    batch() { return ps; },
  }
}

const getConnectionIdQuery = `-- name: GetConnectionId :one
SELECT CAST(json_extract('{"connection_id":"foo"}', '$.connection_id') AS TEXT) AS connection_id`;

export type GetConnectionIdRow = {
  connectionId: string;
};

type RawGetConnectionIdRow = {
  connection_id: string;
};

export function getConnectionId(
  sql: SqlStorage
): Query<GetConnectionIdRow | null> {
  const ps = sql
    .prepare(getConnectionIdQuery);
  return {
    then(onFulfilled?: (value: GetConnectionIdRow | null) => void, onRejected?: (reason?: any) => void) {
      ps.first<RawGetConnectionIdRow | null>()
        .then((raw: RawGetConnectionIdRow | null) => raw ? {
          connectionId: raw.connection_id,
        } : null)
        .then(onFulfilled).catch(onRejected);
    },
    batch() { return ps; },
  }
}

function expandedParam(n: number, len: number, last: number): string {
  const params: number[] = [n];
  for (let i = 1; i < len; i++) {
    params.push(last + i);
  }
  return "(" + params.map((x: number) => "?" + x).join(", ") + ")";
}
